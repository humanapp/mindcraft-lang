# Add `tag:Operator;color:#FFE500`

Adds two numbers together.

Use on the DO side to compute a sum, or on the WHEN side inside a comparison.

## See Also

- `tile:tile.op->sub`
- `tile:tile.op->mul`
- `tile:tile.op->div`
- `tile:tile.op->neg`
