{
  "name": "@mindcraft-lang/typescript",
  "version": "0.1.9",
  "description": "TypeScript-to-Mindcraft bytecode compiler for sensors and actuators implemented in TypeScript",
  "license": "MIT",
  "type": "module",
  "author": "humanapp",
  "repository": {
    "type": "git",
    "url": "https://github.com/humanapp/mindcraft-lang.git",
    "directory": "packages/typescript"
  },
  "bugs": {
    "url": "https://github.com/humanapp/mindcraft-lang/issues"
  },
  "homepage": "https://github.com/humanapp/mindcraft-lang/tree/main/packages/typescript#readme",
  "keywords": [
    "mindcraft",
    "visual-programming",
    "tile-based",
    "typescript",
    "language",
    "education",
    "stem",
    "coding",
    "game-development",
    "creative-coding",
    "kodu",
    "project-spark"
  ],
  "files": [
    "dist",
    "README.md"
  ],
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=18"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "bundle-lib-dts": "node scripts/bundle-lib-dts.js",
    "build": "npm run bundle-lib-dts && tsc -p tsconfig.json",
    "build:prod": "tsc",
    "clean": "tsc --build tsconfig.json --clean",
    "lint:only": "biome lint ./src",
    "check:only": "biome check ./src",
    "format:only": "biome format ./src",
    "lint": "biome lint ./src --write",
    "check": "biome check ./src --write",
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.spec.json",
    "format": "biome format ./src --write",
    "pretest": "npm run build",
    "test": "tsx --tsconfig tsconfig.spec.json --test $(find src -name '*.spec.ts')",
    "release:patch": "npm run build && npm run check:only && node ../../scripts/release.js patch",
    "release:minor": "npm run build && npm run check:only && node ../../scripts/release.js minor",
    "release:major": "npm run build && npm run check:only && node ../../scripts/release.js major"
  },
  "dependencies": {
    "@mindcraft-lang/core": "file:../core",
    "typescript": "~5.7.2"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.15",
    "tsx": "^4.21.0"
  }
}
