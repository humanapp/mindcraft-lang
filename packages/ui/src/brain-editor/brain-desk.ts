/**
 * Fill the editor's page desk is laid on, as a CSS `background` value built
 * from the `--color-brain-desk-*` stops. Put it on any surface that has to read
 * as the same desk.
 */
export const kBrainDeskFill =
  "linear-gradient(160deg, var(--color-brain-desk-from) 0%, var(--color-brain-desk-to) 100%)";
