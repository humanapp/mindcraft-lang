# Assign (=)

Sets a variable to a new value.

Place the variable on the left, the `=` tile in the middle, and the value on the right.

The assignment operator works with all variable types -- numbers, booleans, and strings.

## See Also

- `tile:tile.op->eq`
- `tile:tile.op->ne`
