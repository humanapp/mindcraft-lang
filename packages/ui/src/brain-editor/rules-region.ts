/**
 * Marks the region the editor's rules lay out in. It covers the rules alone:
 * the editor's chrome and the side region stand outside it.
 */
export const kBrainRulesAttribute = "data-brain-rules";
