name: Publish @wendoo-lang/assistant-bridge

on:
  push:
    tags:
      - "assistant-bridge-v*"

permissions:
  contents: write

jobs:
  publish:
    name: Build, Lint, Test, Publish & Release
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: packages/assistant-bridge

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org

      - name: "Rewrite public local file: deps to version ranges"
        working-directory: .
        run: node scripts/rewrite-local-deps.js packages/assistant-bridge

      - name: Install dependencies
        run: npm ci

      - name: Lint & format check
        run: npm run check:only

      - name: Build
        run: npm run build:prod

      - name: Run tests
        run: npm test

      - name: "Strip private local file: deps before publish"
        working-directory: .
        run: node scripts/strip-private-local-deps.js packages/assistant-bridge

      - name: Verify package version matches tag
        run: |
          PACKAGE_VERSION="assistant-bridge-v$(node -p "require('./package.json').version")"
          if [ "$PACKAGE_VERSION" != "${{ github.ref_name }}" ]; then
            echo "Tag ${{ github.ref_name }} does not match package.json version $PACKAGE_VERSION"
            exit 1
          fi

      - name: Publish to npm
        run: npm publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Create GitHub Release
        run: |
          gh release create "${{ github.ref_name }}" \
            --repo "${{ github.repository }}" \
            --title "${{ github.ref_name }}" \
            --generate-notes
        env:
          GH_TOKEN: ${{ github.token }}
