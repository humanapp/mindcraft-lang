name: Publish @wendoo-lang/app-host

on:
  push:
    tags:
      - "app-host-v*"

permissions:
  contents: write

jobs:
  publish:
    name: Lint, Build, Publish & Release
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: packages/app-host

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org

      - name: "Rewrite public local file: deps to version ranges"
        working-directory: .
        run: node scripts/rewrite-local-deps.js packages/app-host

      - name: Install dependencies
        run: npm ci

      - name: Lint & format check
        run: npm run check:only

      - name: Build
        run: npm run build:prod

      - name: "Strip private local file: deps before publish"
        working-directory: .
        run: node scripts/strip-private-local-deps.js packages/app-host

      - name: Verify package version matches tag
        run: |
          PACKAGE_VERSION="app-host-v$(node -p "require('./package.json').version")"
          if [ "$PACKAGE_VERSION" != "${{ github.ref_name }}" ]; then
            echo "Tag ${{ github.ref_name }} does not match package.json version $PACKAGE_VERSION"
            exit 1
          fi

      - name: Publish to npm
        run: npm publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Create GitHub Release
        run: |
          gh release create "${{ github.ref_name }}" \
            --repo "${{ github.repository }}" \
            --title "${{ github.ref_name }}" \
            --generate-notes
        env:
          GH_TOKEN: ${{ github.token }}
